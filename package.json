{
  "name": "GeoTanks",
  "version": "0.1.0",
  "private": true,
  "main": "back-end/server.js",
  "scripts": {
    "start": "cd back-end/ && node server.js",
    "postinstall": "cd front-end/ && npm install && cd ../ && cd back-end/ && npm install && cd .. && cd front-end/ && npm run build && cd ../"
  },
  "license": "MIT",
  "dependencies": {
    "back-end": "file:back-end"
  }
}
