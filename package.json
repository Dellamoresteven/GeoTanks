{
  "name": "GeoTanks",
  "version": "0.1.0",
  "private": true,
  "main": "back-end/server.js",
  "scripts": {
    "start": "npm start --prefix front-end/",
    "postinstall": "npm install --prefix front-end/ & npm install --prefix back-end/"
  },
  "license": "MIT",
  "dependencies": {
    "back-end": "file:back-end"
  }
}
